// SPDX-License-Identifier: PMPL-1.0-or-later
= nickel-augmentation — Outstanding Work
:author: Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>

Outstanding items for future sessions. Completion: ~55%.

== High Priority

* [ ] **Proven bridge** — Document how consumers call proven Idris2 modules (SafeUrl, SafeVersion, SafeString, SafeCrypto/Hash) for runtime enforcement of the same policies Nickel validates at build-time. The Nickel layer declares policy; proven enforces it.
* [ ] **More reporter rules** — Add rules for: SHA-pinned action validation (TYPE-002), container base image policy (Chainguard only), workflow permission checks, Nickel `| default` vs bare field auditing
* [ ] **Integration tests** — Run config-reporter against 3–5 real RSR repos and verify findings match expectations
* [ ] **K9 runner** — Wire `must` shim for Hunt-level K9 execution (currently templates only, no runner)

== Medium Priority

* [ ] **Rewrite config-reporter in Rust** — The bash script works but won't scale to complex rules. A Rust CLI with clap + serde would give structured rule definitions, parallel file scanning, and SARIF output for IDE integration
* [ ] **Nickel package** — Publish augmented/lib/ as a Nickel package when the Nickel package manager matures (currently import-by-path only)
* [ ] **Cross-repo validation** — K9 Yard-level component that typechecks a target repo's .ncl files against augmented/lib/ contracts
* [ ] **shellcheck** — Install shellcheck in CI and fix any warnings in config-reporter/bin/config-reporter

== Low Priority

* [ ] **K9 template copyright headers** — Add proper copyright to K9 templates (currently flagged as META-001 info)
* [ ] **Nickel format workaround** — The `{ .. }` open record syntax doesn't format in Nickel 1.16; monitor upstream for fix
* [ ] **Examples expanding** — Add example for: Cloudflare Workers config, Vercel deployment config, Elixir release config
* [ ] **SLSA / Sigstore integration** — Sign exported JSON configs for supply chain integrity

== Completed (2026-03-16)

* [x] SPDX typo fixes (PLMP → PMPL) across contractiles
* [x] Created augmented/lib/ — 5 modules (rsr, security, ci, infra, prelude)
* [x] Rewrote augmented/README.adoc
* [x] Wired Justfile with real Nickel recipes
* [x] Built config-reporter CLI (9 rules, JSON output, exit codes)
* [x] Fixed infra.ncl merge semantics (| default) and export function (| not_exported)
* [x] Fixed Nickel keyword field names (include/exclude → interpolated)
* [x] All files formatted with nickel format
* [x] `just quality` passes (fmt-check + lint + test)
* [x] panic-attack assail clean (0 unsafe, 0 panics, 1 low-severity TODO finding)
* [x] Added 8 Nickel-specific rules to Hypatia code_safety.ex
* [x] Updated STATE.a2ml, ECOSYSTEM.a2ml, META.a2ml, TOPOLOGY.md
